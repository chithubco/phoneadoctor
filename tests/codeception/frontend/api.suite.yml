# Codeception Test Suite Configuration

# suite for functional (integration) tests.
# emulate web requests and make application process them.
# (tip: better to use with frameworks).

# RUN `build` COMMAND AFTER ADDING/REMOVING MODULES.
#basic/web/index.php
class_name: ApiGuy
modules:
    enabled: [Filesystem, Yii2, Db,REST,PhpBrowser]
    config:
        Yii2:
            configFile: '../config/frontend/functional.php'
        Db:
            dsn: 'mysql:host=localhost;dbname=gaiadb'
            user: 'root'
            password: 'letmein'
            populate: true
            cleanup: false
        REST:
            url: 'http://localhost/phoneadoc/api/web/index.php/v1/'
        PhpBrowser:
          url: http://localhost/phoneadoc/